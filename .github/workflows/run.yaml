name: Run VRP Solvers

on:
  push:
    branches: [ * ]
  pull_request:
    branches: [ * ]
  workflow_dispatch:
    branches: [ * ]

jobs:
  build:
    runs-on: alpine-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - name: Install VRP Solvers
        run: pip install -r requirements.txt
      - name: Run VRP Solvers
        run: python3 solve_vrp.py
